import {Color} from './color';
import StyleConfig from './StyleConfig';
const GS = {
  text_light_blue_bold: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBold,
  },
  text_light_blue_bolditalic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBoldItalic,
  },
  text_light_blue_regular: {
    marginVertical: StyleConfig.smartScale(10),
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_light_blue_italic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontItalic,
  },
  text_light_green_bold: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBold,
  },
  text_light_green_bolditalic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBoldItalic,
  },
  text_light_green_regular: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_light_red_regular: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_gray_regular: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_light_green_italic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontItalic,
  },
  text_white_bold: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBold,
  },
  text_white_bolditalic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBoldItalic,
  },
  text_white_regular: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_white_italic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontItalic,
  },
  text_grey_theme_bold: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBold,
  },
  text_grey_theme_bolditalic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBoldItalic,
  },
  text_grey_theme_regular: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_grey_theme_italic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontItalic,
  },
  text_black_theme_bold: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBold,
  },
  text_black_theme_bolditalic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBoldItalic,
  },
  text_black_theme_regular: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontRegular,
  },
  text_black_theme_italic: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontItalic,
  },
  text_tomato_red_bold: {
    color: Color.white,
    fontSize: StyleConfig.countPixelRatio(18),
    textAlign: 'center',
    fontFamily: StyleConfig.fontBold,
  },
};

export default GS;
